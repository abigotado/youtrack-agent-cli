<!-- Code generated by tools/gencommands. DO NOT EDIT. -->

# Command reference

JSON mode writes one stable envelope to stdout and is the default when stdout is not a TTY. Text, raw, and help output are explicitly selected exceptions. Network commands require an explicit `--profile`; there is no active or default profile. See `contract.md` for exit codes and envelopes.

## JSON response recovery

Apply this grammar in order:

1. A v1 envelope is exactly one complete top-level JSON object followed only by
   whitespace. It requires boolean `ok` and a JSON integer `v` whose value is
   exactly `1`. Decimal, exponent, string, null, boolean, and every other
   version representation is unsupported. If `ok` is `true`, `data` must be
   present and non-null; `error` and `hint` must be absent, and their presence
   is invalid even when `null`. If `ok` is `false`, `data` must be absent;
   `error` must be a present non-null object with string `code` and `message`,
   and `hint` must be a present string. Forbidden-key presence is invalid even
   when `null`. `meta` is optional; unknown additive fields are tolerated. They
   never repair an invalid known member. On either branch, `meta` may be absent;
   when present it must be a non-null object and may be empty. Every present
   known metadata member must
   be non-null and have its exact v1 type: `count` is a nonnegative JSON integer
   written without a fraction or exponent, `truncated` is boolean, and
   `next_cursor`, `profile`, `instance`, `account_id`, and `account_login` are strings. Unknown additive metadata
   members are tolerated, but they never repair a missing, null, or wrongly
   typed known member. A malformed `meta` makes the entire envelope invalid. A
   valid v1 envelope is the primary result; never replace it with stderr. Any
   malformed or unsupported envelope
   makes stdout invalid.

   Valid v1 examples:

   `{"ok":true,"v":1,"data":{},"meta":{},"future":true}`

   `{"ok":false,"v":1,"error":{"code":"PROFILE_REQUIRED","message":"profile is required","future":true},"hint":"pass --profile","meta":{"count":0,"truncated":false,"next_cursor":"","profile":"work","instance":"https://example.youtrack.cloud","future":null},"future":true}`

   Invalid examples:

   - `{"ok":true,"v":2,"data":{}}`
   - `{"ok":true,"v":1.0,"data":{}}`
   - `{"ok":"true","v":1,"data":{}}`
   - `{"ok":true,"v":"1","data":{}}`
   - `{"ok":true,"v":1}`
   - `{"ok":true,"v":1,"data":null}`
   - `{"ok":true,"v":1,"data":{},"error":null}`
   - `{"ok":true,"v":1,"data":{},"hint":null}`
   - `{"ok":false,"v":1,"hint":"stop"}`
   - `{"ok":false,"v":1,"error":null,"hint":"stop"}`
   - `{"ok":false,"v":1,"error":{"code":1,"message":"failed"},"hint":"stop"}`
   - `{"ok":false,"v":1,"error":{"code":"FAILED","message":1},"hint":"stop"}`
   - `{"ok":false,"v":1,"error":{"code":"FAILED","message":"failed"},"hint":1}`
   - `{"ok":false,"v":1,"error":{"code":"FAILED","message":"failed"},"hint":"stop","data":null}`
   - `{"ok":true,"v":1,"data":{},"meta":null}`
   - `{"ok":true,"v":1,"data":{},"meta":[]}`
   - `{"ok":true,"v":1,"data":{},"meta":{"count":null}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"count":-1}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"count":1.0}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"count":1e0}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"count":"1"}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"truncated":null}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"truncated":"false"}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"next_cursor":null}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"next_cursor":1}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"profile":null}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"profile":false}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"instance":null}}`
   - `{"ok":true,"v":1,"data":{},"meta":{"instance":[]}}`

2. A valid v1 stdout envelope is authoritative; stderr is diagnostic only.
3. If a confirmed `mutation apply` result is missing, malformed, or reports
   `WRITE_OUTCOME_UNKNOWN`, never replay the mutation. Run the bounded
   `mutation reconcile` flow for the same receipt. An inconclusive result is
   `operator_resolution_required`, never inferred success or failure.

## Global flags

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--dry-run` | bool |  | preview a supported local or offline change |
| `--fields` | stringSlice |  | comma-separated output fields |
| `-o, --output` | string |  | output format: text, json, or raw |
| `--profile` | string |  | explicit named YouTrack profile |
| `--timeout` | duration | `30s` | abort the command after this duration |
| `-v, --verbose` | bool |  | write redacted request activity to stderr |
| `--yes` | bool |  | confirm a supported local configuration change |

## Commands

### `youtrack-agent-cli auth allow-projects clear`

Remove the local project write policy

No flags of its own.

### `youtrack-agent-cli auth allow-projects set`

Replace the complete exact project allowlist

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--project` | stringArray |  | exact immutable project ID and key as ID:KEY (repeatable) |

### `youtrack-agent-cli auth allow-projects show`

Show the current identity-bound project policy

No flags of its own.

### `youtrack-agent-cli auth import-token`

Verify and store one permanent token from a trusted terminal

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--interactive` | bool |  | read one bounded token from a real terminal with echo disabled |

### `youtrack-agent-cli auth login`

Run public-client OAuth Authorization Code with PKCE

No flags of its own.

### `youtrack-agent-cli auth logout`

Delete one exact credential and its profile metadata

No flags of its own.

### `youtrack-agent-cli auth migrate-keychain`

Rebind one credential ACL to the current application

No flags of its own.

### `youtrack-agent-cli auth status`

Show credential presence and optionally verify the account

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--check` | bool |  | read the exact Keychain item and call users/me |

### `youtrack-agent-cli auth whoami`

Verify and return the exact authenticated account

No flags of its own.

### `youtrack-agent-cli contract`

Print the versioned envelope and exit-code contract

No flags of its own.

### `youtrack-agent-cli inspect issue`

Read one exact issue through the narrow REST helper

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--id` | string |  | exact YouTrack issue database ID or readable ID |

### `youtrack-agent-cli inspect project`

Read one exact project

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--project` | string |  | exact project entity ID or key |

### `youtrack-agent-cli inspect schema`

Read one bounded page of project field schema

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--limit` | int | `50` | maximum field schemas in this page (1-100) |
| `--offset` | int | `0` | explicit bounded page offset |
| `--project` | string |  | exact project entity ID or key |

### `youtrack-agent-cli mutation apply`

Consume one approved plan for at most one mutation attempt

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--plan-id` | string |  | exact local mutation plan ID |

### `youtrack-agent-cli mutation confirm`

Request native trusted-user-presence confirmation

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--plan-id` | string |  | exact local mutation plan ID |

### `youtrack-agent-cli mutation export`

Export one existing journaled plan without network access

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--out` | string |  | new exclusive 0600 plan export path |
| `--plan-id` | string |  | exact local mutation plan ID |

### `youtrack-agent-cli mutation prepare`

Create one complete network-free mutation plan

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--expected-state` | string |  | bounded expected-state JSON file |
| `--kind` | string |  | mutation kind: issue.create, issue.update, or comment.add |
| `--offline` | bool |  | guarantee no credential, browser, or network access |
| `--out` | string |  | new exclusive 0600 plan export path |
| `--project` | string |  | exact allowlisted project as ID:KEY |
| `--request-stdin` | bool |  | read one bounded typed request JSON object from stdin |
| `--schema-sha256` | string |  | lowercase SHA-256 of the inspected project schema |

### `youtrack-agent-cli mutation reconcile`

Collect bounded read-only evidence for an ambiguous outcome

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--plan-id` | string |  | exact local mutation plan ID |

### `youtrack-agent-cli mutation status`

Read one durable local mutation record without network access

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--plan-id` | string |  | exact local mutation plan ID |

### `youtrack-agent-cli profile add`

Add or replace one strict non-secret profile

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--from` | string |  | bounded non-secret profile JSON file |

### `youtrack-agent-cli profile list`

List non-secret profiles without reading Keychain

No flags of its own.

### `youtrack-agent-cli profile remove`

Remove one credential-free profile

No flags of its own.

### `youtrack-agent-cli profile show`

Show one non-secret profile

No flags of its own.

### `youtrack-agent-cli profile validate`

Validate one profile without credentials or network

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--offline` | bool |  | guarantee no credential or network access |

### `youtrack-agent-cli skills install`

Install or update manifest-owned skill files

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--dest` | string |  | explicit existing user-scope skills root |
| `--project-dir` | string |  | existing project root for project scope |
| `--provider` | string | `all` | skill host: codex, claude, or all |
| `--scope` | string | `user` | install scope: user or project |

### `youtrack-agent-cli skills uninstall`

Remove only unchanged manifest-owned skill files

| Flag | Type | Default | Description |
| --- | --- | --- | --- |
| `--dest` | string |  | explicit existing user-scope skills root |
| `--project-dir` | string |  | existing project root for project scope |
| `--provider` | string | `all` | skill host: codex, claude, or all |
| `--scope` | string | `user` | install scope: user or project |

### `youtrack-agent-cli version`

Print the version and Go build stamp

No flags of its own.
